# Project Specification - Next.js Demo Project

> **⚠️ Codex Cloud 执行规范 - 必读**
>
> 如果你是 Codex Cloud 代理在执行任务，请务必阅读并遵守本规范的所有要求。
>
> **项目目标**: 创建一个现代化的 Next.js 示例项目，包含首页和 About 页面

## 0. Codex Cloud 执行要求

### 🚨 npm 镜像配置（CRITICAL - 必须执行）

**由于网络限制，必须使用 npm 淘宝镜像**

项目根目录已包含 `.npmrc` 文件，配置了淘宝镜像：
```
registry=https://registry.npmmirror.com
```

**如果 npm install 出现 403 错误，执行：**
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 🚨 分支命名规范（CRITICAL - 最高优先级）

**强制要求：必须使用以下分支命名格式**

```bash
git checkout -b feature/task-{TASK_ID}
```

**示例：**
- 执行 TASK-001 → 分支名必须是 `feature/task-001`
- 执行 TASK-002 → 分支名必须是 `feature/task-002`
- 执行 TASK-003 → 分支名必须是 `feature/task-003`

**重要说明：**
- ❌ 不能使用 `codex/` 前缀
- ❌ 不能自己创造分支名
- ❌ 不能使用任务描述作为分支名
- ✅ 必须严格使用 `feature/task-{TASK_ID}` 格式
- ✅ TASK_ID 使用三位数字（如 001, 002, 003）

**执行流程：**
1. 开始工作前，先创建并切换到正确的分支
2. 在该分支上完成所有开发工作
3. 提交代码到该分支
4. 推送到远程仓库

### 提交信息规范

```
Complete TASK-{TASK_ID}: {任务描述}
```

示例：
```
Complete TASK-001: 初始化 Next.js 项目并创建导航栏
```

### 工作流程

1. **创建分支**：`git checkout -b feature/task-{TASK_ID}`
2. **开发代码**：按照下面的技术规范完成任务
3. **提交代码**：使用规范的提交信息
4. **推送远程**：`git push -u origin feature/task-{TASK_ID}`

## 1. 技术栈约束

### 必须使用
- **框架**: Next.js 14+ (App Router)
- **语言**: TypeScript 5+
- **样式**: Tailwind CSS 3+
- **包管理器**: npm
- **Node.js**: 18.17.0 或更高版本

### 项目结构
```
letjs-demo/
├── app/
│   ├── layout.tsx              # 根布局
│   ├── page.tsx                # 首页
│   ├── about/
│   │   └── page.tsx            # About 页面
│   ├── globals.css             # 全局样式
│   └── components/
│       └── Navbar.tsx          # 导航栏组件
├── public/
├── next.config.ts
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── postcss.config.mjs
```

## 2. 功能要求

### 导航栏（Navbar）
- Logo: "LetJS Demo"
- 导航链接: Home 和 About
- 白色背景，带阴影效果
- 悬停时底部边框高亮（indigo 色）
- 响应式设计

### 首页（Home）
- **Hero Section**:
  - 大标题: "Welcome to LetJS Demo"
  - 副标题介绍
  - 两个 CTA 按钮（Learn More, Documentation）

- **Features Section**:
  - 三栏卡片网格布局
  - 特性展示: 快速性能⚡、美观设计🎨、类型安全🔧
  - 悬停时阴影增强效果

- **Tech Stack Section**:
  - 技术栈标签展示
  - 包含: Next.js 14, TypeScript, Tailwind CSS, App Router

### About 页面
- **页面头部**: 清晰的标题和描述
- **项目介绍**: 白色卡片，介绍项目目标
- **关键特性**: 四个功能点卡片（App Router🚀、TypeScript📘、Tailwind🎨、响应式📱）
- **技术栈**: 两栏网格展示技术分类
- **CTA 区域**: 渐变背景，包含返回首页和 GitHub 链接按钮

## 3. 设计要求

### 颜色方案
- **主色**: Indigo（#4F46E5 - indigo-600）
- **次色**: Purple（#9333EA - purple-600）
- **背景**: 白色/深色渐变（支持深色模式）
- **文字**:
  - 主文字: gray-900 (深色模式: white)
  - 次文字: gray-600 (深色模式: gray-300)

### 响应式设计
- **移动端** (< 640px): 单栏布局
- **平板** (640px - 768px): 两栏布局
- **桌面端** (> 768px): 三栏布局
- 使用 Tailwind 断点: sm:, md:, lg:, xl:

### 深色模式支持
- 所有组件支持深色模式
- 使用 Tailwind 的 `dark:` 前缀
- 背景渐变适配深色主题

### 交互效果
- 按钮悬停: 颜色加深
- 卡片悬停: 阴影增强（shadow-md -> shadow-xl）
- 导航链接悬停: 底部边框显示
- 平滑过渡动画: transition-colors, transition-shadow

## 4. 代码质量标准

### 必须遵守
- [ ] 代码结构清晰，易于阅读
- [ ] 使用语义化的变量和函数命名
- [ ] 所有组件使用 TypeScript
- [ ] 确保类型安全，无 any 类型
- [ ] 使用 Next.js 最佳实践

### 命名规范
- 组件: PascalCase (Navbar, HomePage)
- 函数: camelCase (handleClick, fetchData)
- 文件: kebab-case 或 PascalCase (navbar.tsx 或 Navbar.tsx)
- CSS类: Tailwind utility classes

### TypeScript 要求
- 明确定义组件 Props 类型
- 使用 `import type` 导入类型
- 使用 Next.js 提供的类型（Metadata, ReactNode）

### Next.js 最佳实践
- 使用 App Router 文件约定
- 使用 Server Components（默认）
- 使用 Metadata API 设置 SEO
- 使用 `next/font` 优化字体加载
- 使用 `next/link` 进行路由导航

## 5. SEO 和可访问性

### SEO 优化
- 每个页面定义独立的 metadata
- 使用语义化 HTML 标签
- 合理的标题层级（h1 -> h2 -> h3）

### 可访问性
- 使用正确的 HTML 标签（nav, main, section, article）
- 链接和按钮文字清晰明确
- 颜色对比度符合 WCAG AA 标准

## 6. 初始化配置

### create-next-app 配置
```bash
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*" --eslint --no-git
```

配置选项：
- TypeScript: Yes
- ESLint: Yes
- Tailwind CSS: Yes
- App Router: Yes
- src/ directory: No
- Import alias: @/*
- Initialize Git: No (已有 Git 仓库)

### 注意事项
- 会覆盖现有的 README.md
- 保留现有的 Git 配置
- 保留 SPEC.md 和 PLAN.md 文件

## 7. 验证标准

### 功能验证
- [ ] 开发服务器正常启动（npm run dev）
- [ ] 首页正确渲染所有区域
- [ ] About 页面正确渲染所有区域
- [ ] 导航链接工作正常
- [ ] 响应式布局在不同屏幕下正常
- [ ] 悬停效果正常工作

### 构建验证
- [ ] 生产构建成功（npm run build）
- [ ] 无 TypeScript 类型错误
- [ ] 无 ESLint 错误
- [ ] 生产模式正常运行（npm start）

## 8. 常见问题处理

### 目录冲突
- create-next-app 会警告目录中有文件
- 需要确认覆盖（或手动删除冲突文件）
- 保留 SPEC.md, PLAN.md, .codex-env 等重要文件

### 端口占用
```bash
# 使用其他端口
npm run dev -- -p 3001
```

### Tailwind 样式不生效
- 确保 globals.css 包含 @tailwind 指令
- 重启开发服务器

## 9. 项目元信息

- **项目名称**: LetJS Demo
- **描述**: A modern Next.js application with TypeScript and Tailwind CSS
- **作者**: Generated by Claude Code with SDCL Mode
- **许可**: MIT
